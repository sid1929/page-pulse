{
  "name": "page-pulse",
  "version": "1.0.0",
  "description": "Production-grade URL audit service — checks a target URL's availability, performance, and basic SEO/meta health.",
  "type": "commonjs",
  "main": "src/server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/**/*.test.js"
  },
  "license": "MIT"
}
